void main() {
  print("The sum of 4 and 5 is:${sum(4, 5)}");
  print("The multiply of 10 and 5 is:${multiply(10, 5)}");
}

sum(x, y) {
  var z = x + y;
  return z;
}

multiply(x, y) {
  var z = x * y;
  return z;
}
